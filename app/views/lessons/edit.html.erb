<div class="card">
  <div class="card-header">
    <h1>Editing lesson</h1>
  </div>
  <div class="card-body">
    <%= render "form", lesson: @lesson %>
  </div>
  <div class="card-footer">
    <%= link_to "Show this lesson", course_lesson_path(@lesson.course_id, @lesson) %> |
    <%= link_to "Back to the course", course_path(@course) %>
  </div>
</div>
