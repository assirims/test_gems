<!DOCTYPE html>
<html>
  <head>
    <title>
      <%= content_for?(:title) ? yield(:title) : "Website" %>
    </title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- fontawesome -->
    <script src="https://kit.fontawesome.com/9f14ba6cba.js" crossorigin="anonymous"></script>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application.bootstrap", "data-turbo-track": "reload" %>
    <%= favicon_link_tag 'logo.png' %>
    <!-- use this line below if gem 'chartkick' is not working properly -->
    <%#= javascript_include_tag "http://www.google.com/jsapi", "chartkick" %>
    <%= javascript_importmap_tags %>
    <%= render 'layouts/google_analytics' if Rails.env.production? %>
  </head>

  <body>
    <%= render "layouts/navbar" %>
    <%= render "layouts/notifications" %>
    <div class="container"><br />
      <%= yield %>
    </div>
    <br />
    <%= render "layouts/footer" %>
  </body>
</html>
